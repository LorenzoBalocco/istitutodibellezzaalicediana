# Open Graph — Istituto di Bellezza Alice Diana

**Data:** 2026-02-13-16-50-23
**Agenti:** seo-specialist → copywriter
**Stato:** COMPLETATO ✓

---

## Obiettivo

Completare l'implementazione Open Graph del sito prima del commit su GitHub:
1. Generare `og-image.jpg` (1200×630) dalla hero section via screenshot Chrome headless
2. Scrivere copy OG ottimizzato (title + description) per homepage e pagine interne
3. Aggiornare `layout.tsx` e i layout delle pagine interne con i nuovi tag

---

## Stato attuale OG

Da `layout.tsx` (riga 38–46):
```ts
openGraph: {
  type: "website",
  locale: "it_IT",
  siteName: "Istituto di Bellezza Alice Diana",
  title: "Istituto di Bellezza Alice Diana — Trino",
  description: "Centro estetico a Trino Vercellese dal 1992. ...",
  // ⚠️ MANCA: og:image
  // ⚠️ MANCA: og:url
}
```
Manca l'immagine OG e `og:url`. Twitter card e description ci sono ma vanno ottimizzate.

Pagine interne (`/trattamenti`, `/chi-siamo`, `/prodotti`, `/contatti`) hanno layout propri ma nessuna con `openGraph.images`.

---

## Piano di implementazione

### Step 1 — Screenshot hero section (og-image)

- Avviare dev server (`npm run dev`)
- Chrome headless: `--window-size=1200,630 --screenshot` su `http://localhost:3000`
- Viewport 1200×630 = hero riempie tutto lo schermo → screenshot perfetto della hero
- Output: `public/og-image.jpg`

### Step 2 — seo-specialist: audit OG attuale

Analisi degli attuali meta tag OG e raccomandazioni per:
- title length (50–60 chr)
- description length (150–160 chr)
- image format/size
- og:url, og:locale
- Twitter card completamento

### Step 3 — copywriter: scrittura copy OG

Copy ottimizzato in italiano per:
- Homepage: title + description OG
- `/trattamenti`: title + description OG
- `/chi-siamo`: title + description OG
- `/prodotti`: title + description OG
- `/contatti`: title + description OG

### Step 4 — Aggiornamento codice

File da modificare:
| File | Modifica |
|------|---------|
| `src/app/layout.tsx` | + `openGraph.images`, `openGraph.url`, Twitter card aggiornato |
| `src/app/trattamenti/layout.tsx` | + `openGraph.images` |
| `src/app/chi-siamo/layout.tsx` | + `openGraph.images` |
| `src/app/prodotti/layout.tsx` | + `openGraph.images` |
| `src/app/contatti/layout.tsx` | + `openGraph.images` |

---

## In attesa di conferma

Rispondi **"procedi"** per avviare l'implementazione.
